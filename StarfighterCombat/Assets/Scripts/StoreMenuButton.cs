/*using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class StoreMenuButton : MonoBehaviour
{
    public GameObject StartGameButton;
    public GameObject RightArrowButton;
    public GameObject LeftArrowButton;
    public GameObject CraftMenuSelector;
    public GameObject Levels;

    public StoreButton StoreButton;

    private void Start()
    {
        StoreButton.onClick.AddListener(OnStoreButtonClicked);
    }

    private void OnStoreButtonClicked()
    {
        StartGameButton.SetActive(!StartGameButton.activeSelft);
        RightArrowButton.SetActive(!RightArrowButton.activeSelft);
        LeftArrowButton.SetActive(!LeftArrowButton.activeSelft);
        CraftMenuSelector.SetActive(!CraftMenuSelector.activeSelft);
        Levels.SetActive(!Levels.activeSelft);
    }
}
*/